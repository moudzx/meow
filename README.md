# meow
first install mpg123
<code> sudo apt install mpg123 </code>
move your mp3 file to the same directory, or change the filepath in the code
<code> gcc meow.c -o meow && ./meow </code>

stop the program with Ctrl+Z
<code> pkill -9 meow </code>
